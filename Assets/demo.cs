/*
	Ліцензія: CC-BY
	Автор: Василь ( wmysterio )
	Сайт: http://metal-prog.zzz.com.ua/
*/
using MyRPG.Items;
using MyRPG.Configuration;
using System.Text;
using UnityEngine;
using UnityEngine.UI;

namespace MyRPG.Assets {

    public sealed class demo : MonoBehaviour {
        
        private void Start() {
            //var content = gameObject.GetComponent<Text>();
            //StringBuilder builder = new StringBuilder();
            //builder.AppendLine( "Звичайний текст" );
            //for( int i = 0; i < 11; i++ )
            //    builder.AppendLine( Colors.WrapString( "Зразок #1 ( Colors.Mode )", ( Colors.Mode ) i ) );
            //for( int i = 0; i < 6; i++ )
            //    builder.AppendLine( Colors.WrapString( "Зразок #2 ( TypeOfItemRarity )", ( TypeOfItemRarity ) i ) );
            //for( int i = 0; i < 3; i++ )
            //    builder.AppendLine( Colors.WrapString( "Зразок #3 ( RelationshipOfPersonage )", ( RelationshipOfPersonage ) i ) );
            //content.text = builder.ToString();
        }

    }

}